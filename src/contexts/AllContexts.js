import { createContext } from "react";

export const AddBillContext = createContext(null);

export const AddFriendsContext = createContext(null);

export const DisplaySharesContext = createContext(null);